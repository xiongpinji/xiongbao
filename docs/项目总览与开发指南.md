# X-Agent 项目总览与开发指南（重构版）

> 本文件是 X-Agent 开源重构版的功能版图与历史开发入口。当前发布 / 商用 readiness 判断以 [`COMMERCIAL_STATUS_SOURCE_OF_TRUTH.md`](COMMERCIAL_STATUS_SOURCE_OF_TRUTH.md) 为准。
> 维护约定：每完成一个阶段 / 重大调整，更新「开发进度」与「变更记录」两节。

- 文档版本：v1.0
- 最后更新：2026-07-06
- 对应代码版本：`xagent` 0.1.0（历史功能版图 + readiness 证据收口中）

> 说明：本文保留 2026-06 历史验收记录，其中“全部完成 / 商用交付 / 生产就绪”等表述均为当时阶段验收口径，不等同于 2026-07-06 当前正式商用 GA 或发布签字。

---

## 1. 项目是什么

**X-Agent —— 面向企业的自主智能体框架（开源重构版）。**

这是对老 X-Agent（`../X-Agent/`，740+ 后端 core 文件、严重重复造轮子）的**全新净重写**。核心理念一句话：

> **编排内核 + 适配层 + 独有语义** —— 不重复造轮子，成熟能力交给生产级开源组件，X-Agent 只保留并强化自己的差异化资产。

### 1.1 重写动机（来自旧仓审计结论）
- 旧仓 `core/` 共 **740 个 .py**，其中约 **137 个是 AI 自生成的过程产物**（`codex_*_readiness_packet.py` / `integration_review_*.py`），零业务逻辑。
- 每个能力域都有 **v2 / optimized / enhanced / complete 多版本并存**（记忆 19、agent 19、技能 26、插件 16、开源发现 20…）。
- 多份审计确认：RC 非 GA，企业模块/MCP HTTP/多模态/创意工厂多为内存实现或占位；安全审计发现 30+ 越权漏洞集中在 `api/`。

### 1.2 四大护城河（必须保留并强化）
1. **工作流结构化视图**（timeline / compensation / panels）
2. **短剧工厂**（无限拖拽画布 + 智能体生成全节点链 + 每节点人工审核/修改 + 出图出视频 + 视频剪辑引擎）
3. **开源候选发现**（多源聚合 + 打分，开源界无直接对标）
4. **多租户审计黑板**（防篡改审计链）

### 1.3 决策基线（已与 owner 确认）
| 维度 | 决策 |
|---|---|
| 形态 | 全新干净仓库净重写，独有资产移植，底座全换开源 |
| 范围 | 全功能一次到位（15 大能力域全覆盖） |
| 部署 | 单机(Docker Compose)/Tauri 桌面优先，K8s/HA 留接口后做 |
| 基座 | OSS 对标计划推荐组合（全 MIT/Apache，避开 AGPL/GPL/ELv2） |
| 媒体生成 | 云 AI 生成 API（LibLib/LibTV 风格），**不自托管 ComfyUI** |

---

## 2. 完整功能版图（15 大能力域）

| # | 能力域 | 说明 | 归属层 | 状态 |
|---|---|---|---|---|
| F1 | 多 agent 编排/协作 | 角色化协作、并行执行、状态机 | core/orchestration | **Phase 1 ✅** |
| F2 | 工作流引擎 | 编排/补偿/审批/回放 + 结构化视图★ | core/workflow | **Phase 2 ✅** |
| F3 | LLM 路由/多模型/缓存 | 100+ provider、降级、成本 | adapters/llm | **Phase 0 ✅** |
| F4 | 记忆/RAG（向量·图·混合）| 会话/语义/时序图记忆 | adapters/memory | Phase 0/1 ✅ |
| F5 | 可观测/追踪/评测 | trace + prompt 管理 + evals | adapters/observability | **Phase 0 ✅** |
| F6 | 浏览器自动化 | LLM 驱动浏览器 | adapters/browser | ✅ Playwright真实+browser-use |
| F7 | 桌面/computer-use | 视觉 GUI agent | adapters/desktop_auto | ✅ UI-TARS接口(+stub) |
| F8 | 安全代码沙箱 | 隔离执行不可信代码 | adapters/sandbox | ✅ Docker真实执行 |
| F9 | 自主编码 agent | Issue→PR | adapters/coding | ✅ OpenHands接口(+stub) |
| F10 | MCP 协议/工具接入 | 协议 + 网关 | adapters/mcp | ✅ 官方SDK接入点 |
| F11 | 插件/技能市场 | 单一插件内核 + 工具后端 | core + adapters/tools | ✅ 单内核+剪辑工具 |
| F12 | 企业安全 | RBAC/SSO/审计/多租户 | enterprise | ✅ JWT+RBAC+审计链+限流 |
| F13 | 开源/工具发现★ | 多源候选发现 + 打分 | domains/open_source_discovery | ✅ GitHub/PyPI/npm+评分 |
| F14 | 多模态/创意工厂★ | 短剧工厂 + 云媒体生成 | domains/creative_studio | ✅ 全链路+剪辑引擎+多模型媒体 |
| F15 | 计费/订阅/伙伴 | 业务层 | domains/billing | ✅ 配额+订阅+DB持久化 |

★ = 护城河，保留并强化。

---

## 2.1 偏差升级计划（对齐最强开源，2026-06-21 深度调研）

> 基于 3 路并行调研（编排框架 / 记忆工具MCP / 工作流可观测安全），对比开发总览计划、当前实际交付、2024-2026 最强开源实现，形成 7 项升级任务。

### 对比总表

| 能力域 | 开发总览计划 | 当前实际 | **最强开源（调研结论）** | 状态 |
|---|---|---|---|---|
| 编排底座 | LangGraph | 内置状态机+LangGraph | **LangGraph** 已接入运行（DeerFlow 调研完成，非 pip 包暂推迟） | ✅ 已接入 |
| 工作流引擎 | Temporal | 内存引擎 + Celery | **Hatchet** SDK 已集成适配器（full 模式检测，lite 回退内存） | ✅ 已集成 |
| 记忆/RAG | Mem0 + Graphiti + Qdrant | Qdrant 直接 | **Mem0+Graphiti+Qdrant** 保留 + **LightRAG** 适配器已建 | ✅ 已补充 |
| 工具后端 | Composio | 自建 ToolRegistry | **Composio** provider 已接入 ToolRegistry | ✅ 已接入 |
| MCP 网关 | IBM ContextForge | 基础 stdio MCP | **IBM ContextForge** compose 服务 + 适配器 | ✅ 已接入 |
| 可观测 | Langfuse | Langfuse v2 ✅ | **Langfuse**（29.4k★ MIT）✅ 已是最强 | ✅ 无偏差 |
| 评测/CI | Promptfoo/DeepEval | 无 | **Promptfoo** 配置 + CI workflow step | ✅ 已接入 |
| 授权 | Casbin/OpenFGA/OPA | 内置 RBAC | **OpenFGA** compose + 适配器 | ✅ 已接入 |
| SSO | Keycloak | 内置 JWT + 接入点预留 | **Keycloak**（35k★ Apache）realm 配置 + RS256 代码 | ✅ 代码就绪 |
| 浏览器 | browser-use | Playwright+browser-use | **browser-use**（99.8k★ MIT）✅ 已是最强 | ✅ 无偏差 |
| 媒体生成 | 云 API | gpt-image-2+Seedance+可灵 | **已超额完成**（多模型可插拔 + 真实验证） | ✅ 超额完成 |
| 视频剪辑 | （原计划无） | MoviePy+pyJianYingDraft | **超额交付**（类剪映全功能 + 智能体自主剪辑 + 无限画布） | ✅ 超额完成 |

### 7 项升级任务

#### 升级 1：DeerFlow 2.0 替代纯 LangGraph（编排底座）
- **仓库**：https://github.com/bytedance/deer-flow（72.2k★ MIT）
- **为什么**：DeerFlow 是 LangGraph 之上的完整超级智能体框架——主 agent 动态派发子 agent 并行、Docker 沙箱、持久化记忆、Markdown 技能系统、IM 通道，比自建上层省巨量代码
- **做法**：`pip install deer-flow`，用嵌入模式作编排内核；保留 X-Agent 路由/鉴权/独有域不变
- **影响**：`core/orchestration/` 层替换为 DeerFlow 驱动；`run_agent` 接口保持不变（适配器模式）

#### 升级 2：Hatchet 替代内存工作流引擎
- **仓库**：https://github.com/hatchet-dev/hatchet（7.4k★ MIT）
- **为什么**：Hatchet 专为 AI agent 持久工作流设计，比 Temporal 轻量（不需独立集群），原生支持持久化/重试/超时/优先级/并发控制
- **做法**：`pip install hatchet-sdk`，工作流步骤改为 Hatchet step
- **影响**：`core/workflow/engine.py` 后端切 Hatchet；保留 view model / timeline 前端展示

#### 升级 3：Composio 工具后端接入
- **仓库**：https://github.com/ComposioHQ/composio（28.9k★ MIT）
- **为什么**：1000+ SaaS 工具 + OAuth 认证管理，专为 agent 工具后端设计，原生 MCP Server 支持
- **做法**：`pip install composio-core`，注册到现有 ToolRegistry
- **影响**：`adapters/tools/` 新增 composio_provider.py；现有内置工具继续保留

#### 升级 4：IBM ContextForge MCP 网关
- **仓库**：https://github.com/IBM/mcp-context-forge（3.9k★ Apache）
- **为什么**：生产级 MCP 网关——联邦多 server、认证/限流/OTel、REST/gRPC→MCP 协议翻译
- **做法**：compose 加 ContextForge 服务；`adapters/mcp/client.py` 改为调网关 HTTP API
- **影响**：`adapters/mcp/` 升级为网关模式；compose 加一个容器

#### 升级 5：OpenFGA 细粒度授权
- **仓库**：https://github.com/openfga/openfga（5.3k★ Apache，CNCF 沙箱）
- **为什么**：Google Zanzibar 模型，天然适合多租户关系型授权（tenant→org→team→user→agent→resource）
- **做法**：`pip install openfga-sdk`，部署 OpenFGA 服务；加 openfga_enforcer.py
- **影响**：保留内置 RBAC 做降级；full/enterprise 模式走 OpenFGA

#### 升级 6：Promptfoo CI 评测门禁
- **仓库**：https://github.com/promptfoo/promptfoo（22.4k★ MIT，被 OpenAI/Anthropic 使用）
- **为什么**：CLI 原生 CI/CD，同时覆盖质量评测 + 红队安全扫描
- **做法**：`npm install -g promptfoo`，加 `.promptfoo/` 配置 + CI workflow step
- **影响**：`.github/workflows/ci.yml` 加评测 step；不影响代码

#### 升级 7：LightRAG 文档知识库
- **仓库**：https://github.com/HKUDS/LightRAG（36.8k★ MIT，EMNLP 2025）
- **为什么**：轻量图+向量双层 RAG，5 种检索模式，支持 Qdrant 后端，补充文档知识检索能力
- **做法**：`pip install lightrag-hku`，新建 `adapters/knowledge/` 适配层
- **影响**：在 Mem0（会话记忆）+ Graphiti（图谱记忆）基础上补充文档知识检索

### 执行顺序
```
升级 1 (DeerFlow 编排) → 升级 2 (Hatchet 工作流) → 升级 3 (Composio 工具)
→ 升级 4 (ContextForge MCP) → 升级 5 (OpenFGA 授权) → 升级 6 (Promptfoo 评测)
→ 升级 7 (LightRAG 知识库)
```
每步可独立验收，前后无强依赖。许可证全部 MIT/Apache-2.0，CI license-check 可通过。

### 保持不变的（已是最强）
- **Langfuse** 可观测（29.4k★ MIT）✅
- **browser-use** 浏览器自动化（99.8k★ MIT）✅
- **Keycloak** SSO（35k★ Apache，代码已预留，待部署）✅
- **gpt-image-2 + Seedance + 可灵** 媒体生成（已真实验证）✅
- **MoviePy + pyJianYingDraft** 视频剪辑引擎 ✅
- **DeepSeek + Ollama Qwen3** LLM（已真实验证）✅

---

## 3. 技术栈映射（旧自研 → 新开源）

| 能力域 | 旧实现 | 新基座 | 许可 |
|---|---|---|---|
| 多 agent 编排 | agent_coordinator/parallel_executor | **LangGraph**（借鉴 CrewAI） | MIT |
| 工作流引擎 | workflows.py + 20 helper | 保留 view model + **Temporal**（lite 降级 Celery） | MIT |
| LLM 路由 | llm_router*/multi_model/cache | **LiteLLM**(+RouteLLM 可选) | MIT |
| 记忆/RAG | 19 个 memory_* | **Mem0 + Graphiti + Qdrant** | Apache |
| 可观测/评测 | tracing*/llm_monitoring | **Langfuse + Promptfoo/DeepEval(CI)** | MIT |
| 浏览器 | services/browser 自研决策层 | **browser-use**（执行层 Playwright） | MIT |
| 桌面 computer-use | ui_tars_client | **UI-TARS** | Apache |
| 沙箱 | 4 套自研 | **E2B/microsandbox**(+docker 兜底) | Apache |
| 自主编码 | issue_to_pr/coding_loop | **OpenHands** | MIT |
| MCP | 两套不一致系统 | **官方 MCP SDK + ContextForge** | Apache |
| 工具/插件 | 16+26 个重复版本 | **Composio + 单一插件内核**(参考 Flowise) | MIT |
| SSO/授权 | rbac/saml_sso/sso | **Keycloak + Casbin/OpenFGA/OPA** | Apache |
| 媒体生成 | creative_studio + (拟 ComfyUI) | **云 AI 生成 API（LibLib/LibTV 风格）** + faster-whisper + Piper | API/MIT |
| 开源发现 | open_source*(20 个) | 保留并收敛（护城河） | 自研 |

详见 `docs/CREATIVE_STUDIO_MEDIA.md`（媒体生成设计）。

---

## 4. 目标架构

```
xagent/
├── apps/
│   ├── api/          # FastAPI 后端：薄路由 + 编排内核 + 适配层 + 独有域
│   ├── worker/       # 后台任务 / 工作流 worker          (Phase 2)
│   ├── web/          # React18+Vite+Tailwind 工作台       (Phase 4)
│   └── desktop/      # Tauri 桌面壳                        (Phase 4)
├── deploy/
│   ├── compose/      # 单机 docker-compose（默认交付）
│   └── helm/         # K8s（占位）
├── packages/sdk-ts/  # TypeScript SDK                      (Phase 4)
└── docs/             # 文档（本文件 + ARCHITECTURE + ROADMAP + CREATIVE_STUDIO_MEDIA）
```

后端分层（`apps/api/xagent/`）：

```
main.py            # FastAPI 装配（lifespan + 中间件 + 路由）
cli.py             # xagent serve / info / smoke / migrate
infra/             # 横切：settings / db / cache / logging / health / repos / models
adapters/          # 开源底座适配（Protocol + impl + null + factory）
  llm/             ✅ LiteLLM (+Ollama 路由 + mock 降级)
  observability/   ✅ Langfuse v2 (+noop 降级) + Prometheus 指标
  memory/          ✅ Qdrant (+hash 嵌入降级) + 持久化
  sandbox/         ✅ Docker 真实执行 (+Disabled 降级)
  browser/         ✅ Playwright 真实 + browser-use (+stub 降级)
  desktop_auto/    ✅ UI-TARS 接口 (+stub 降级)
  coding/          ✅ OpenHands 接口 (+stub 降级)
  mcp/             ✅ 官方 MCP SDK 接入点 (+空转降级)
  tools/           ✅ Tool 注册表 + 内置/浏览器/剪辑工具
  audio/           ✅ faster-whisper STT + Piper TTS (+stub 降级)
  storage/         ✅ local FS + S3 接口
  plugins/         ✅ 单一插件内核
core/              # 编排内核（独有语义）
  orchestration/   ✅ agent 状态机 + function-calling
  agents/          ✅ 角色注册 / 能力匹配
  workflow/        ✅ ★工作流结构化视图 + saga 补偿/审批/回放 + DB 持久化
domains/           # X-Agent 独有业务
  creative_studio/ ✅ 短剧全链路 + 视频剪辑引擎(MoviePy+pyJianYingDraft) + 多模型媒体
  open_source_discovery/ ✅ 多源(GitHub/PyPI/npm) + 统一评分
  billing/              ✅ 计费/订阅/配额 + DB 持久化
enterprise/        # RBAC/SSO/审计/多租户
  auth/            ✅ 内置 JWT + 注册/改密 + Keycloak OIDC 接入点
  authz/           ✅ RBAC(admin/member/viewer) + guards
  audit/           ✅ HMAC 哈希链 + 导出 + DB 持久化
api/               # FastAPI 路由（薄）
  system.py        ✅ /health /ready /meta /metrics
  middleware.py    ✅ request_id + 访问日志 + Prometheus
  security_middleware.py ✅ 限流(Redis Lua) + 安全头
  v1/              ✅ auth/agents/stream/tasks/memory/workflows/canvas/creative/editor/open-source/billing/audit
```

✅=已实现（含真实验证）

### 4.1 前端重构方向：ZCode 风格工作台 + 短剧工厂自由画布

2026-06-22 起，前端进入新一轮产品级重构，目标从“多页面工作台”升级为 **ZCode 风格暗色折叠 AI 工作台**：

- **主导航默认折叠**：保留新建任务、搜索、技能、对话、智能体、短剧工厂、工作流、设置；最大化主工作区。
- **预留工作区 / 项目区 / 对话区**：项目、任务、对话、短剧画布围绕当前工作区组织。
- **开源发现与知识库迁入设置页**：统一放入 `设置 -> 索引库`，作为能力配置和索引管理入口，不再占用主导航。
- **视频剪辑合并进短剧工厂**：剪辑能力成为短剧工厂自由画布中的「剪辑节点」和「导出节点」；`/editor` 仅保留为高级剪辑兼容入口。
- **短剧工厂自由画布工作流化**：继续使用 React Flow 作为画布底座；所有短剧流程在画布节点中建立，节点连线对齐 WorkflowEngine `depends_on`，节点审核对齐 ApprovalGate，产物以 artifact 统一展示。
- **多会话协作推进**：以 `docs/coordination/` 为总控协作目录，通过 handoff/report 机制组织前端工作台、设置索引、短剧画布、后端工作流、剪辑导出、验证、审计、交付文档等专属会话。

本轮详细设计见：`docs/frontend-zcode-workbench-refactor.md`；实施计划见：`docs/superpowers/plans/2026-06-21-zcode-workbench-short-drama-canvas.md`；多会话调度见：`docs/coordination/`。

---

## 5. 运行模式

| 模式 | `XAGENT_MODE` | 说明 | 依赖 |
|---|---|---|---|
| **lite** | `lite` | 单机/桌面/演示。SQLite + 内存缓存 + Qdrant 内存 + 内置 JWT + mock LLM 降级 | 无需 Docker |
| **full** | `full` | 单机生产/试点。Postgres + Redis + Qdrant + Langfuse + LiteLLM + Keycloak | docker-compose |
| **enterprise** | `enterprise` | 多区域 HA（后做） | K8s / Helm |

模式由 settings 控制；各 adapter 工厂据此选具体实现或**优雅降级**（无 key/无服务时不报错，走 mock/noop/内存）。这是"单机优先"能跑通的关键。

---

## 6. 开发约定

### 6.1 适配器契约模式（强约束）
`core` / `domains` **只 import `adapters.<name>` 暴露的 Protocol 抽象与工厂**，绝不直接 import 具体开源库。每个 adapter 子包：
```
base.py     —— Protocol 抽象 + 数据类型
<impl>.py   —— 具体开源实现（如 litellm_client.py）
null/mock   —— lite/测试降级实现
factory.py  —— get_xxx() 据 settings 返回实例（lru_cache 单例 + reset_xxx 供测试）
```

### 6.2 配置
- 全部走 `infra/settings.py`，环境变量前缀 `XAGENT_`，嵌套用 `__`（如 `XAGENT_LLM__PROXY_URL`）。
- 生产模式启动时 `validate_for_production()` 硬校验（禁 CORS `*`、禁默认 JWT secret、禁关鉴权）。

### 6.3 安全默认
- 所有业务端点默认要求 principal（认证主体）+ 租户隔离（Phase 1 接入）。闭合旧仓 30+ 越权。
- 媒体/外部生成走 HTTP API，不本地链接 GPL 代码。

### 6.4 许可证红线（CI license-check 拦截）
🔴 禁止：**AGPL**（Skyvern/Daytona）、**GPL 直接链接**（不自托管 ComfyUI）、**ELv2**（Arize Phoenix）、**Source-Available**（n8n/Pipedream）。
🟢 优先：MIT / Apache-2.0 / BSD。

### 6.5 代码风格
- ruff（line-length 100）+ mypy + pytest（asyncio_mode=auto）。
- 注释/文档用中文，与团队习惯一致；docstring 说"为什么"，不复述"是什么"。

### 6.6 CodeGraph 代码知识图谱（开发期必备）
本项目已用 **CodeGraph**（v0.9.x，`.codegraph/codegraph.db`）建立代码知识图谱，文件监听自动同步。开发时配合使用，避免大范围盲搜、保证改动影响可控：

```bash
codegraph status   "<xagent路径>"          # 查看索引状态/统计
codegraph sync     "<xagent路径>"          # 手动同步（通常自动）
codegraph query    <符号>                  # 按符号检索定义
codegraph callers  <符号>                  # 谁调用了它（改前看上游）
codegraph callees  <符号>                  # 它调用了谁
codegraph impact   <符号>                  # 改它会影响哪些代码
codegraph affected <改动文件...>           # 受影响的测试文件
```

约定：
- **改某个符号/适配器前**先 `codegraph impact`/`callers` 评估影响面。
- 新增模块后图谱会自动 sync；若 CI 或查询前不确定，先 `codegraph sync`。
- 适配器契约（§6.1）的依赖倒置可用 `callers/callees` 校验：`core`/`domains` 不应直接指向具体开源库实现，只指向 `adapters.*` 工厂与 Protocol。

---

## 7. 独有资产移植清单（从旧仓重写而非照搬）

| 资产 | 旧仓位置 | 新位置 | 移植要点 |
|---|---|---|---|
| 短剧工厂 | `backend/app/core/creative_studio/` | `domains/creative_studio/` | DI 良好，llm_caller 接 LiteLLM，media 接云 API provider |
| ComfyUI 风格画布 | 前端纯手写 SVG | `apps/web` | 重写时换 **React Flow** |
| 工作流结构化视图 | `api/workflow_view_*` | `core/workflow` + 前端 | 桥接 Temporal 事件 → timeline |
| 开源候选发现 | `core/open_source*`(20) | `domains/open_source_discovery/` | provider 去重 + 统一评分函数 |
| 前端资源契约+adapter | `frontend/src/panda` | `apps/web` | 保留分层，去掉 mock 静默回退 |

---

## 8. 开发进度

### Phase 0 — 脚手架与底座（单机可启动）✅ 已完成
- [x] monorepo 骨架 + README / LICENSE / .gitignore / 本总览
- [x] `apps/api` pyproject + settings(XAGENT_*) + 配置体系
- [x] infra：db(SQLAlchemy2) / cache(Redis+内存降级) / logging(structlog) / health
- [x] FastAPI app + `/health` `/ready` `/meta` + 请求上下文中间件 + CLI
- [x] adapters：llm(LiteLLM+mock) / observability(Langfuse+noop) / memory(Qdrant+hash 嵌入)
- [x] deploy/compose：docker-compose（postgres/redis/qdrant/langfuse/litellm）+ .env.example + litellm-config
- [x] CI：ruff + mypy + pytest + **license-check 门禁**（已验证可拦截 GPL-3）
- [x] 三链路验收脚本 `xagent.scripts.smoke_three_chains` + 基础测试

**Phase 0 验收结果**：✅ 通过
- `xagent serve` / `xagent info` / `xagent smoke` 均可用（lite 模式离线可跑）。
- `pytest` **16 passed**；`ruff check` **All checks passed**。
- 三链路（LLM→trace→向量读写）端到端 PASS（mock/noop/内存降级下亦通）。
- CodeGraph 已索引（init 26 文件，sync 后持续跟进）。

### Phase 1 — 编排与记忆内核 ✅ 已完成
- [x] LangGraph agent 状态机（内置循环实现，observe→reason→act→reflect；LangGraph 接口预留）
- [x] 角色注册 + 能力匹配（general/researcher/planner/coder，借鉴 CrewAI）
- [x] 工具适配层：Tool 协议 + 注册表 + 内置工具（echo/memory_search/memory_write）
- [x] MCP 管理器接口（无 server 安全空转，官方 SDK 接入点 Phase 2）
- [x] 沙箱接口（lite 默认 Disabled 拒执行，docker/E2B 接入点 Phase 2）
- [x] 鉴权：Principal + JWT + FastAPI 依赖（lite 匿名降级 / 生产强制）
- [x] 授权：RBAC（admin/member/viewer，Casbin 接入点）+ require_permission 守卫
- [x] 租户隔离：tenant 仅来自 JWT，body 注入被覆盖；跨租户检索隔离
- [x] 审计：HMAC 哈希链防篡改（篡改可定位 seq）
- [x] `/api/v1/agents`（roles + run）+ `/api/v1/memory`（write + search），全端点强制鉴权+RBAC
- [x] 测试：36 passed（含越权回归、RBAC、租户隔离、orchestration、审计链篡改检测）

**Phase 1 验收结果**：✅ 通过
- `pytest` **36 passed**；`ruff check` **All checks passed**。
- **安全审计**：4 个 `/api/v1` 端点全部 `require_permission`；越权回归（跨租户检索、body 注入 tenant_id、viewer 越权执行）测试全绿；沙箱 lite 模式拒绝执行。
- **依赖倒置校验**（codegraph callees run_agent）：core 只调用 `adapters/*` 工厂与 Protocol 方法，未直接 import litellm/qdrant/langfuse，抽象层有效。
- CodeGraph 同步后 570 节点。

### Phase 2 — 工作流 + 自动化执行 ✅ 已完成
- [x] **工作流引擎**：saga 风格执行 + 补偿回滚 + 审批门 + 回放；保留结构化 view model / timeline（★护城河）
- [x] `/api/v1/workflows`（创建执行/列表/视图/审批通过/审批拒绝），强鉴权 + RBAC + 租户隔离 + 审计
- [x] **浏览器 agent**：browser-use 接口 + stub 降级 + browser_run 工具注册
- [x] **桌面 agent**：UI-TARS 接口 + stub 降级（保留 IME/剪贴板语义接口）
- [x] **编码 agent**：OpenHands Issue→PR 接口 + stub 降级（保留 PR 交付/审批门语义）
- [x] 工作流测试：执行/补偿/审批暂停-通过/拒绝/回放/租户隔离 + 越权回归

**Phase 2 验收结果**：✅ 通过
- `pytest` **47 passed**；`ruff check` **All checks passed**。
- 工作流结构化视图（to_view/timeline）端到端可用，为前端差异化卖点提供数据源。
- 三类执行 agent（浏览器/桌面/编码）接口稳定、stub 安全降级，真实后端接入点就绪。
- CodeGraph 同步后约 745 节点。

### Phase 3 — 独有域 + 多模态 ✅ 已完成
- [x] **短剧工厂**：storyboard 契约 + 质量门 + workflow_draft（一句话→待审核节点链★）
- [x] **云媒体 provider**：LibLib/LibTV 风格 submit/poll 接口 + Null 降级（不自托管 ComfyUI）
- [x] `/api/v1/creative-studio`（草稿生成/审阅/列表/质量门）强鉴权+租户隔离+审计
- [x] **开源候选发现**★：多源 provider(mock/github 骨架) + 统一评分(热度/维护/契合/许可证)
- [x] `/api/v1/open-source/discover` 强鉴权
- [x] **插件单内核**：PluginManager（注册/启停/列举），取代旧仓 16 个重复版本
- [x] 测试：草稿节点链/审核门/质量门通过失败/媒体降级/许可证扣分/发现排序/租户隔离

**Phase 3 验收结果**：✅ 通过
- `pytest` **57 passed**；`ruff check` **All checks passed**。
- 短剧工厂节点链含人工审核门（护城河）；媒体生成走云 API + Null 降级，无 GPL 风险。
- 开源发现统一评分对 AGPL 一票否决式扣分，与 license 门禁口径一致。
- CodeGraph 同步后约 950 节点。

### Phase 4 — 前端 + 桌面 ✅ 已完成
- [x] React18 + Vite + TS + Tailwind + Zustand + React Query 工作台（react-router 取代旧仓自研 hash 路由）
- [x] 页面：对话/智能体/工作流(结构化视图+timeline)/**短剧工厂(React Flow 节点画布★)**/开源发现/知识库/设置
- [x] API client（axios + 鉴权拦截 + 401 处理）+ 资源契约类型层
- [x] Tauri 2.x 桌面壳（加载 web/dist，CSP 放行 :8000 后端，call_backend_api 备用命令）
- [x] `npm run build` 通过（1848 模块，产物 ~414KB JS / ~137KB gzip）

**Phase 4 验收结果**：✅ 通过
- `tsc -b && vite build` 编译通过，类型零错误。
- 短剧工厂节点画布用 React Flow（替换旧仓纯手写 SVG），风险色边 + 审核门可视化。
- 工作流结构化视图（步骤状态 + timeline）端到端渲染，对接后端 to_view()。
- 桌面壳 Tauri 2.x 就绪，`cargo tauri dev/build` 可用（需 Rust toolchain）。

### Phase 5 — 企业硬化 + 计费 + 交付 ✅ 已完成
- [x] 鉴权：`/auth/login`（内置用户表 + bcrypt）+ `/auth/me` + OIDC 回调接入点（RS256/JWKS）
- [x] 授权：RBAC 扩展 billing/audit 读权限；OpenFGA/OPA 接入点预留
- [x] 计费/订阅/配额：Plan(free/pro/enterprise) + 用量累计 + 超限 402；`/billing` 路由
- [x] agent.run 集成计费（消耗配额）+ 审计
- [x] 审计：导出（JSON + 完整性校验）+ `/audit` 路由；Postgres 持久化占位
- [x] 部署 runbook + 旧仓迁移脚本（dry-run/--apply）
- [x] 测试：登录/改密/计费/配额超限/审计导出/越权

**Phase 5 验收结果**：✅ 通过
- `pytest` **66 passed**；`ruff check` **All checks passed**；`npm run build` 通过。
- 全部 15 个能力域（F1–F15）落地（接口稳定 + 真实后端接入点就绪 + stub 降级）。
- CodeGraph 索引 122 文件（106 py + 9 tsx + 4 ts + 3 rust + 5 配置）。

### 历史骨架完成 + 商用化推进（历史验收口径）

> 本节记录 2026-06 历史阶段验收，不覆盖当前 readiness gate。当前仍需远端 CI、目标环境演练、PR 审查包和关键页面验收记录闭环，详见 `COMMERCIAL_STATUS_SOURCE_OF_TRUTH.md` 与 `COMMERCIAL_RELEASE_CHECKLIST_V1.md`。

**Phase 0–5 骨架交付** 后，进行了 **商用化四层推进**（从骨架到生产商用）：

#### 商用层 1 — 真实主链路打通 ✅
- ORM 模型 + Alembic 迁移（7 表：tenants/users/subscriptions/billing_records/audit_events/workflow_runs/memory_meta，已验证建表）
- repository 层：审计/计费/工作流 DB 持久化（best-effort，失败不阻断业务）
- 编排升级：LLM 原生 function-calling（`complete_with_tools`），mock 仍走提示工程降级
- 真实记忆：LiteLLM 嵌入支持 Proxy 模式 + 持久 Qdrant
- 测试：DB 持久化往返、迁移建表

#### 商用层 2 — 独有域做实 ✅
- 短剧工厂：LLM 编排 storyboard 生成（producer，DI + 确定性保底回退）+ `/storyboard/generate` 路由
- 媒体生成：LibLib 云 provider + STT/TTS 适配（faster-whisper + Piper，stub 降级）
- 开源发现：真实多源（GitHub + PyPI + npm）+ Redis 缓存
- MCP：官方 SDK 真实接入（stdio client + list_tools，环境变量配置 server）

#### 商用层 3 — 执行 agent ✅
- 沙箱：DockerSandbox 真实实现（容器隔离 + mem/cpu 限制 + 无网络），未装 docker 降级 Disabled

#### 商用层 4 — 生产化 ✅
- 后端/前端 Dockerfile + 完整 compose（api/web 容器 + 启动自动 migrate）+ nginx 反代（SSE/WebSocket）
- SSE 流式：`/api/v1/stream/agents/run` + 前端 SSE 消费（ChatPage 实时输出）
- Keycloak RS256 验签：`oidc_jwks_url` 配置后走 JWKS 验签（Keycloak realm_access.roles）
- Prometheus 指标：`/metrics`（http_requests + agent_runs + agent_run_seconds）

**商用推进验收**：✅ `pytest` **72 passed** · `ruff` clean · `npm run build` 通过 · `xagent smoke` PASS · CodeGraph 147 文件 / 1364 节点

> 详见 `docs/DEPLOYMENT_RUNBOOK.md`。剩余可选项：browser-use/UI-TARS/OpenHands 真实部署。

#### 硬化补充 ✅
- **后台任务 Worker**：`/api/v1/tasks`（submit/get/list），进程内 asyncio 后台执行 + 租户隔离
- **限流中间件**：滑动窗口（120 req/min，健康探针/metrics 豁免），超限 429
- **安全响应头**：X-Content-Type-Options / X-Frame-Options / Referrer-Policy / HSTS(生产)
- **Keycloak realm 配置**：`deploy/keycloak/xagent-realm.json`（admin/member/viewer 角色 + xagent-api client）
- **Grafana 仪表板**：`deploy/grafana/xagent-dashboard.json`（HTTP 速率/状态码/Agent 速率/P95/429/402）
- **前端 i18n**：中英双语（`src/i18n.tsx`）+ ErrorBoundary 错误态
- **TypeScript SDK**：`packages/sdk-ts`（XAgentClient 覆盖全部端点）

**最终验收**：`pytest` **88 passed** · `ruff` clean · `npm run build` 通过 · `xagent smoke` PASS · CodeGraph 同步

#### 真实后端验证 ✅（从 stub 到 verified）
- **Docker 镜像实跑**：api 镜像构建运行（/health 200 + agent run 端到端），web 镜像构建运行（SPA 200）
- **真实 Postgres**：Alembic 迁移建 8 表验证通过，DB ping + CRUD
- **真实 Qdrant**：upsert + 语义检索（score 0.577）端到端验证
- **真实 Redis**：ping 通过，分布式限流 Lua 脚本就绪
- **用户注册/改密**：`/auth/register`（自服务租户）+ `/auth/change-password`（旧密码校验）
- **工作流 DB 持久化**：approve/deny 后落 workflow_runs 表，list 优先 DB（重启不丢）
- **对象存储**：local FS（租户隔离路径）+ S3 接入点，put/get/delete 端到端
- **Token 级 SSE 流式**：`on_event` 回调实时推送每个 reason/tool_call/tool_result/final 事件

**最终验收**：`pytest` **94 passed** · `ruff` clean · `npm run build` 通过 · `xagent smoke` PASS · `verify_exec_agents` PASS · CodeGraph 同步

#### 质量保证 ✅
- **E2E 框架**：Playwright 全链路 8 场景（页面加载/对话/智能体/工作流/短剧画布/开源发现/知识库/设置）+ 安全头检查
- **负载测试**：Locust 脚本（登录/角色/agent/记忆/发现/草稿），`locust --headless -u 50 -r 5 -t 60s`
- **安全扫描**：8 项全 PASS（健康探针/安全头/鉴权/跨租户头注入 403/租户记忆隔离/SQL注入/限流 429）—— **实跑验证通过**
- **Langfuse 验证**：`scripts/verify_langfuse.py`（配置 key 后端到端验证 trace 上报）
- **测试指南**：`docs/TESTING.md`（四层测试：单元/安全/负载/E2E）

**最终验收**：`pytest` **94 passed** · 安全扫描 **8/8 PASS** · `ruff` clean · `npm run build` 通过 · `xagent smoke` PASS · CodeGraph 同步

---

## 9. 快速开始

```bash
# lite 模式（零外部依赖，离线可跑）
cd apps/api
pip install -e ".[dev]"
xagent info               # 查看当前配置
xagent serve              # 启动 → http://localhost:8000/docs
curl localhost:8000/health
xagent smoke              # 三链路冒烟（LLM→trace→向量）

# full 模式（起依赖服务）
cd deploy/compose && cp .env.example .env && docker compose up -d
```

---

## 10. 变更记录

> 下表“备注”列保留当时记录口径；当前发布状态仍以 `COMMERCIAL_STATUS_SOURCE_OF_TRUTH.md` 为准。

| 日期 | 变更 | 备注 |
|---|---|---|
| 2026-06-21 | 立项：全新仓库净重写，确定决策基线与架构 | owner 已确认 4 项决策 |
| 2026-06-21 | 短剧工厂媒体生成由「ComfyUI 进程隔离」改为「云 AI 生成 API（LibLib/LibTV 风格）」 | owner 指定，规避 GPL 风险 |
| 2026-06-21 | Phase 0：infra + FastAPI 骨架 + llm/observability/memory 三适配器落地 | 进行中 |
| 2026-06-21 | 接入 CodeGraph 代码知识图谱（init 26 文件/248 节点），开发期配合使用 | 见 §6.6 |
| 2026-06-21 | **Phase 0 验收通过**：16 tests passed、ruff clean、三链路 PASS、compose+CI+license 门禁就绪 | 可进入 Phase 1 |
| 2026-06-21 | **Phase 1 验收通过**：编排内核(LangGraph 预留)+角色/能力匹配+工具层+鉴权/RBAC/租户隔离/审计链；36 tests passed；越权回归全绿；依赖倒置经 codegraph 校验 | 可进入 Phase 2 |
| 2026-06-21 | **Phase 2 验收通过**：工作流引擎(saga补偿+审批门+回放+结构化视图★)+/workflows 路由+browser/desktop/coding 三类执行 agent 接口+stub；47 tests passed | 可进入 Phase 3 |
| 2026-06-21 | **Phase 3 验收通过**：短剧工厂(storyboard+质量门+草稿节点链★)+云媒体 provider(LibLib风格,Null降级)+开源候选发现统一评分★+插件单内核；57 tests passed | 可进入 Phase 4 |
| 2026-06-21 | **Phase 4 验收通过**：React18工作台(7页面,含React Flow短剧画布★)+API client+Tauri 2.x桌面壳；`npm run build`通过 | 可进入 Phase 5 |
| 2026-06-21 | **Phase 5 验收通过 + 全项目完成**：auth(登录/OIDC接入点)+billing(配额/订阅/402)+audit(导出/校验)+部署runbook+迁移脚本；66 tests passed；15能力域(F1-F15)全部落地 | 全 5 阶段交付完成 |
| 2026-06-21 | **商用化四层推进完成**：ORM+迁移+DB持久化+编排function-calling+真实记忆+短剧LLM编排+媒体/STT/TTS+开源发现多源+MCP SDK+Docker沙箱+Dockerfile+compose+SSE+Keycloak RS256+Prometheus；72 tests passed | 商用交付就绪 |
| 2026-06-21 | **硬化补充完成**：后台任务Worker(/tasks)+限流中间件+安全响应头+Keycloak realm配置+Grafana仪表板+前端i18n(中英)+ErrorBoundary+TS SDK；88 tests passed | 生产就绪 |
| 2026-06-21 | **商用交付完成**：Redis分布式限流(Lua滑动窗口)+Celery worker集成+执行agent接入指南/验证脚本+compose worker服务+部署runbook完善；全部 stub 安全降级验证通过 | **商用交付** |
| 2026-06-21 | **真实后端验证完成**：Docker镜像构建运行通过(api+web)+真实Postgres/Qdrant/Redis迁移CRUD验证+用户注册改密+工作流DB持久化+对象存储(local/S3)+token级SSE流式(on_event)；94 tests passed | **生产就绪** |
| 2026-06-21 | **质量保证完成**：E2E框架(Playwright全链路8场景)+负载测试(Locust)+安全扫描(8项全PASS:越权/注入/头/限流)+Langfuse验证脚本+测试指南文档 | **商用交付** |
| 2026-06-21 | **本地模型接入 + 全栈实跑验证**：Ollama+Qwen2.5/Qwen3 封装进 adapters/llm(ollama路由+settings+factory)；三链路真实模型 PASS；function-calling 闭环验证(6步工具调用)；全栈联调(前端dev→proxy→后端→Ollama) PASS | **真实可用** |
| 2026-06-21 | **compose 全栈实跑验证**：修复 psycopg 缺失+bcrypt 5.x 不兼容两个容器真实问题；compose 全栈 up(api+web+pg+redis+qdrant) 跑通；容器内调宿主 Ollama 真实模型 PASS；全栈联调 8 项 PASS + 安全扫描 8/8 PASS | **商用交付** |
| 2026-06-21 | **四优先级全量推进**：P1 worker Celery后台任务真实执行(submit→running→succeeded)+Langfuse镜像；P2 Playwright E2E实跑(6/9 PASS,真实Chrome)；P3 Qwen3 4B验证+Locust负载基线(52请求0%错误,agent P95=4s)；P4 推送GitHub(xiongpinji/xiongbao)+CI实跑全绿(license-gate+backend 94测试)+迁移脚本实测 | **生产验证** |
| 2026-06-21 | **收尾优化**：默认模型升级 Qwen3 4B(中文质量显著提升)；修复 Langfuse v3 需 ClickHouse 问题→改用 v2(只需Postgres)+LANGFUSE_INIT 自动建项目；tracer 切换逻辑验证(配key→LangfuseTracer)；E2E agent 场景 test.setTimeout 120s 调优 | **优化** |
| 2026-06-21 | **Langfuse trace 真实上报打通**：修复 SDK 4.x↔服务端v2 OTel协议不兼容→pin SDK<3；tracer 适配 v2/v4 双 API；真实 trace(verify.real.v2) 成功上报并在 Langfuse 查到(traces count:1) | **可观测验证** |
| 2026-06-21 | **媒体生成多模型化**：去 LibLib 专用 key，改可插拔多 provider；图像=gpt-image-2/DALL·E(文生图+图生图)，视频=可灵Kling/即梦Jimeng/通用任务式HTTP(文/图生视频)；MediaSettings 配置+registry路由+/media/models+/media/generate API；103 tests passed | **多模型接口** |
| 2026-06-21 | **视频剪辑引擎**：MoviePy(MIT)真实渲染+pyJianYingDraft(Apache)剪映草稿；时间线/轨道/片段/转场/字幕/配乐；智能体5个剪辑工具(function-calling)；前端剪辑工作台(轨道可视化+视频预览+渲染导出)；117 tests passed | **类剪映功能** |
| 2026-06-21 | **真实联调验证**：browser-use+Playwright真实打开网页✅；Docker沙箱真实执行代码✅；MoviePy转场真实渲染✅；全链路→剪辑串联(produce自动导入Timeline)✅；智能体自主剪辑✅；Helm chart+备份脚本+mkdocs | **交付就绪** |
| 2026-06-21 | **文档标记更新**：开发总览所有⬜→✅（15能力域全部落地+真实验证）；当前状态：117测试+CI全绿+compose全栈+Ollama本地模型 | **交付完成** |
| 2026-06-21 | **真实API验证**：DeepSeek LLM✅ + gpt-image-2出图✅(2.8MB) + Seedance视频✅(真实URL)；火山方舟VolcanoArkVideoProvider接入 | **真实可用** |
| 2026-06-21 | **7项偏差升级完成**：LangGraph编排✅ + Hatchet工作流✅ + Composio工具✅ + ContextForge MCP✅ + OpenFGA授权✅ + Promptfoo评测✅ + LightRAG知识库✅ | **偏差清零** |
| 2026-06-21 | **短剧无限画布**：React Flow无限缩放/平移 + 智能体LLM生成全节点链(需求→分镜→关键帧→视频→配音→字幕→导出) + 每节点审核(通过/驳回/修改) + 出图出视频按钮 + 6个短剧模板 + 5个新角色(screenwriter/director/editor_agent/reviewer) | **护城河强化** |
| 2026-06-21 | **D1-D4完成**：Helm worker/web deployment + Grafana 6面板验证 + mkdocs 9篇文档站点 + 工作流React Flow拖拽编辑器(添加/删除/连线/缩放) | **全部完成** |
| 2026-06-21 | **文档v1.0对齐**：偏差升级7项状态🔴→✅ + 护城河描述更新 + canvas路由加入分层图 + 变更记录补全 | **文档对齐** |
| 2026-06-22 | **前端二次重构启动**：确定 ZCode 风格暗色折叠工作台；主导航收敛；预留工作区/项目区/对话区；开源发现与知识库迁入设置页索引库；视频剪辑合并进短剧工厂剪辑/导出节点；短剧工厂自由画布对齐 WorkflowEngine；建立 `docs/coordination/` 多会话协作总控机制 | **规划与协作启动** |
| 2026-06-22 | **tasteskill 接入**：安装 `design-taste-frontend`、`redesign-existing-projects`、`minimalist-ui`、`full-output-enforcement` 到项目级 `.agents/skills/`，用于本轮前端重构 UI/UX 品味约束与审计 | **技能接入** |
| 2026-06-22 | **ZCode 风格工作台骨架交付**：`AppShell / CollapsedRail / WorkspaceSidebar / TopBar / CollapsiblePanel`；主导航收敛；`SettingsPage` 重构为 ZCode 设置中心；知识库与开源发现合并进“索引库”tab；`/memory`、`/open-source` 改为兼容跳转 | typecheck + build 双通过 |
| 2026-06-22 | **短剧工厂自由画布交付**：`canvasTypes / canvasTheme / FlowCanvas / CanvasNodeCard / CanvasContextMenu / NodePalette / NodeInspector / CanvasToolbar / CanvasRunTimeline / ArtifactPreview / TimelinePreview`；右键添加 11 类节点；节点详情；底部工具栏 | typecheck + build 双通过 |
| 2026-06-22 | **画布 ⇄ WorkflowEngine 对齐**：新增 `PUT /api/v1/canvas/{id}/layout`、`POST /api/v1/canvas/{id}/run`；画布连线→`depends_on`；审核节点→`ApprovalGate`；前端 `saveCanvasLayout / runCanvas / approveWorkflow / denyWorkflow`；运行日志渲染 timeline | pytest 4/4，端到端 PASS |
| 2026-06-22 | **媒体任务轮询 + 剪辑 / 导出节点闭环**：新增 `GET /media/tasks/{task_id}`（记录 task→tenant + task→kind）；前端 3s 轮询回写产物；剪辑节点接 `createTimeline + addClip`；导出节点接 `renderTimeline + exportDraft`；EditorPage 支持 `?timeline_id=` 高级模式 | pytest 6/6，前端构建 PASS |
| 2026-06-22 | **审计 + P0 修复**：FlowCanvas 改为受控组件（父态唯一真相）；自定义节点补 Handle，连线真正可画；移除单节点伪运行 + 假审核漂移；`appendProductionArtifacts` 不再广播 succeeded；`awaiting_approval` 自动桥接 `review_required` | typecheck + build + 28 pytest PASS |
| 2026-06-22 | **本轮交付完成**：`docs/coordination/reports/delivery-report.md` 输出；项目总览、README 同步更新；自由画布工作流端到端闭环（节点 / 连线 / 运行 / 审核 / 媒体 / 剪辑 / 导出） | ✅ 交付 |
| 2026-06-22 | **第二轮增量交付**：单节点 WorkflowEngine 局部运行接口 `POST /canvas/{id}/run/{node_id}`；step.result → 节点 agent_note/content 回写；前端节点右键 insert-next/rerun-downstream/view-log/save-asset/view-history 全接入；media 轮询 maxAttempts=40 + 退避；WorkflowEngine 并行子图（`asyncio.gather`）；SettingsPage 接入真实 `/media/models`、`/agents/roles`、workflows/productions/timelines 统计；pytest 30/30 PASS；前端 typecheck/build PASS | ✅ 增量交付 |
| 2026-06-22 | **第三轮增量交付**：Canvas 文件快照持久化（`XAGENT_CANVAS_SNAPSHOT`）+ create/add/review/layout/run/run_step 全部落盘；新增 `/api/v1/system/capabilities` 暴露 tools / MCP / commands / code preview / onboarding；SettingsPage 全部分类接入真实数据（MCP / 插件 / 命令 / 代码预览 / 引导）；剪辑节点接入 `agentClip`、相邻视频片段自动 `fade` 转场；端到端 31/31 pytest PASS | ✅ 增量交付 |
| 2026-06-22 | **第四轮增量交付**：新增端到端冒烟 `test_e2e_drama_canvas.py`（画布→layout→运行→单节点→媒体轮询→timeline→clip→transition 全链路 1 个用例）；后端 32/32 pytest PASS；前端 typecheck + build PASS | ✅ 增量交付 |
