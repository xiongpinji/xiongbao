# REVIEW 积压项验收材料包（R8/R14/R15/R16/R17/R18/R19）

> 日期：2026-08-03
> 整理：Kimi CLI 会话（受 owner 委托整理，**验收/退回决定权在 owner/总调度**）
> 背景：7 项 REVIEW 自 2026-07-06/07 挂起至今；期间分支推进 221 提交（见 R5 包 v2 附录）。本包逐项复核材料在**当前现实**下是否仍成立，并给出验收建议。
> 边界：本包只整理与建议；不直接把 REVIEW 改为 DONE（任务板状态变更由 owner 确认后执行）。

---

## 总览

| 项 |  bounded purpose | 当前复核结果 | 建议 |
|---|---|---|---|
| R8 对外口径一致性终检 | 发布口径文档统一指向 SOT，无过度发布表述 | ✅ 今日重跑扫描通过 | **可验收** |
| R14 Vite chunk 拆包 | 路由级懒加载清除 chunk warning | ✅ 今日重跑构建通过 | **可验收** |
| R15 证据同步 | 任务板/delivery-report/R8 审计一致性修正 | ✅ 历史修正仍成立 | **可验收** |
| R16 full-mode 演练前置清单 | R4 恢复输入清单 | ✅ 已被 U2/R31 实际消费 | **可验收** |
| R17 PR 证据矩阵源数据 | R5 输入源 | ✅ 已被 R5 包（含 v2）消费 | **可验收** |
| R18 候选新鲜度审计 | 判定"需冻结新候选 + 新 CI" | ✅ 其 gate 要求已达成 | **可验收** |
| R19 secret 交接模板 | 无密交接模板（R4 输入） | ✅ 模板无真实 secret | **可验收** |

---

## 逐项复核

### R8 对外口径一致性终检包 —— 建议验收

- **复核动作（2026-08-03）**：重跑其 §6 扫描命令的等价检查：
  - 过度发布/陈旧口径扫描（README、ROADMAP：`Phase 0（当前）`、`唯一权威入口`、`全项目完成`、`商用交付`、`生产就绪`）——**0 命中**；
  - `R17 READY` / `下一张 Codex READY` / `PR 审查包已组装完成` / `目标环境演练已完成` 残留扫描（R8/R15/R17 三文）——**0 命中**。
- **其边界仍成立**：R8 从未宣称 GA / R4 完成 / R5 完成。
- **验收注意（不阻断）**：R8 的统一口径成文于 7/6，此后新增的 SSO/沙箱/Helm/容量证据未回写 SOT/README 的"当前状态"段；如需对外使用这些证据，按 R8 既有机制另开口径刷新包即可。

### R14 Vite chunk 拆包 —— 建议验收

- **复核动作（2026-08-03）**：
  - `npm run build` 退出码 0，20.95s，**无 chunk warning**；
  - 当前最大 chunk `markdown-BsXECrJR.js` 333.61 kB（< 500 kB 阈值）；
  - `vite.config.ts` `chunkSizeWarningLimit` 仍为默认 500（未调高阈值掩盖）；
  - `App.tsx` 仍有 13 处 `lazy(`（路由级懒加载未回退）。
- **其 reviewer 关注点全部满足**：是路由级拆包而非调阈值；build warning 清零未被误写成性能签字。

### R15 证据同步 —— 建议验收

- **复核**：其修正（R8 IN_PROGRESS→REVIEW、R10/R11/R12 不再写成 READY、R13 REVIEW→DONE、R18/R19 依赖标签漂移显式化）在当前任务板与 delivery-report 中仍保持；其"不把 REVIEW 转 DONE"的边界未被违反。
- **注意（不阻断）**：其状态矩阵中 `R4 BLOCKED / R5 BLOCKED` 是 7/7 时点快照，此后 U2/R31（R4 当前机器等价演练）与 R5 包已落地——属后续进展，不是 R15 的错误。

### R16 full-mode 演练前置清单 —— 建议验收

- **复核**：清单（env 模板、三选一 LLM 路径、显式账号、端口矩阵、恢复步骤）完整且已被 U2/R31（7/7-7/8 当前机器 full-mode 等价环境实跑）实际消费，达成其"R4 恢复输入"的 bounded purpose。
- **注意（不阻断）**：8 月新增 secretRef 外部密管（944675b）与 Keycloak realm 补全后，secret 注入多了 `SECRETREF:file:/env:` 路径；如需再做一轮 R4 级演练，可在 R16 清单上增补该路径（另开小包）。

### R17 PR 证据矩阵源数据 —— 建议验收

- **复核**：其作为 R5 输入源数据的使命已完成——R5 包（7/8）及其 v2 附录（8/3）均已基于该矩阵组装；其"源数据不等于最终 PR 文案"的边界始终被遵守。
- **注意（不阻断）**：矩阵 §8（性能与容量）当时为"Mostly open"，现已有正式压测证据（`audit-20260802/LOAD_TEST_FORMAL_20260803.md`）；§2.4/2.6 也可补充 SSO/沙箱/e2e-api 转绿证据。矩阵刷新建议另开包，不回写本历史包。

### R18 候选新鲜度审计 —— 建议验收

- **复核**：其核心判定"R5 不能把 PR #6/`d59faa3` 的 CI 当作当前工作树证据；需冻结新候选并重跑远端 CI"已被后续执行满足：
  - 候选分支 PR #7 持续冻结推进，当前 HEAD `cfb973d`；
  - 远端 CI run `30829673045`（commit `402302a`）与 cfb973d 最新 run 均 success（backend/frontend/license-gate/e2e-api）。
- **其剩余风险条款（依赖标签漂移记录）仅为如实记录，无需动作**。

### R19 secret 交接模板 —— 建议验收

- **复核**：模板全文占位符化，扫描无任何真实 secret 模式（sk-/JWT/长 base64/password 赋值）——**0 命中**；其"是 R4 输入而非演练证据"的边界明确。
- **注意（不阻断）**：模板的实际填写属目标环境/发布负责人动作；结合 secretRef（见 R16 注意项）后可作为下一轮 R4 级演练的交接载体。

---

## 建议的验收后动作（owner 确认后执行）

1. 任务板 7 项 REVIEW → DONE，逐项标注"2026-08-03 依据本包验收"；
2. 建议另开的后续小包（不阻塞本次验收）：
   - 口径刷新包：把 SSO/沙箱/Helm/容量证据按 R8 机制回写 SOT/README；
   - R17 矩阵刷新包：§2.4/2.6/§8 补 SSO/沙箱/e2e/压测证据；
   - R16 增补包：secretRef 注入路径入清单；
   - e2e-api 纳入后端变更常跑门禁（避免再次长期 SKIPPED 积压漂移）。

## 复核证据

- R8 扫描：本仓库 2026-08-03 grep 复核，0 命中（命令见其 §6 等价形式）；
- R14 构建：`apps/web && npm run build` 2026-08-03，exit 0 / 20.95s / 无 chunk warning / 最大 chunk 333.61 kB；
- R19 扫描：secret 模式 grep 0 命中；
- R18 对照：`gh pr view 7`（head cfb973d，checks 全 SUCCESS）+ run `30829673045`；
- 任务板现状：`docs/coordination/TASK_BOARD.md`（7 项 REVIEW 条目及其 live 行）。
