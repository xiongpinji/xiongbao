{{- if .Values.worker.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-worker
  labels: { app: xagent-worker }
spec:
  replicas: {{ .Values.worker.replicas }}
  selector:
    matchLabels: { app: xagent-worker }
  template:
    metadata:
      labels: { app: xagent-worker }
    spec:
      containers:
        - name: worker
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          env:
            - { name: XAGENT_MODE, value: "enterprise" }
            - { name: XAGENT_DB__URL, value: "{{ .Values.postgres.url }}" }
            - { name: XAGENT_CACHE__REDIS_URL, value: "{{ .Values.redis.url }}" }
            - { name: XAGENT_MEMORY__QDRANT_URL, value: "{{ .Values.qdrant.url }}" }
            - { name: XAGENT_LLM__OLLAMA_MODEL, value: "{{ .Values.llm.ollamaModel }}" }
            - name: XAGENT_SECURITY__JWT_SECRET
              value: {{ required "security.jwtSecret is required and must be a 32+ character random value" .Values.security.jwtSecret | quote }}
          command: ["celery", "-A", "xagent.worker.celery_app", "worker", "--loglevel=info", "--concurrency", "{{ .Values.worker.concurrency }}"]
          resources:
            requests: { cpu: "250m", memory: "512Mi" }
            limits: { cpu: "1000m", memory: "2Gi" }
{{- end }}
---
{{- if .Values.web.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-web
  labels: { app: xagent-web }
spec:
  replicas: 2
  selector:
    matchLabels: { app: xagent-web }
  template:
    metadata:
      labels: { app: xagent-web }
    spec:
      containers:
        - name: web
          image: "xagent-web:{{ .Values.image.tag }}"
          ports: [{ containerPort: 80 }]
          resources:
            requests: { cpu: "100m", memory: "128Mi" }
            limits: { cpu: "500m", memory: "256Mi" }
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-web
spec:
  selector: { app: xagent-web }
  ports: [{ port: 80, targetPort: 80 }]
{{- end }}
---
{{- if .Values.observability.prometheus }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-prometheus-config
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s
    scrape_configs:
      - job_name: xagent-api
        kubernetes_sd_configs:
          - role: pod
        relabel_configs:
          - source_labels: [__meta_kubernetes_pod_annotation_prometheus_io_scrape]
            action: keep
            regex: true
{{- end }}
